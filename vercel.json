{
  "version": 2,
  "buildCommand": "npm install && npx vite build --config vite.config.vercel.ts",
  "outputDirectory": "dist/public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
